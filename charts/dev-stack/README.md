# ORT Server Dev Stack

> [!CAUTION]
> This Helm chart uses insecure defaults and is only ment for local development.
> It should never be used in production.

This Helm chart provides a minimal stack of dependencies required to deploy the ORT Server Helm chart to a Kind cluster for local development.
